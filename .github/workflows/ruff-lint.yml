# TODO: Enable this once we have a ruff config
# Currently disabled - only runs manually
name: Ruff Lint Check

on:
  workflow_dispatch:  # Manual trigger only - effectively disabled

jobs:
  ruff-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install ruff
        run: pip install ruff==0.9.4

      - name: Run ruff lint check
        run: ruff check .
